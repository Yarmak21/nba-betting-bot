Вам потрібно оновити файл workflow на GitHub. Ось що потрібно зробити:

📝 Оновіть файл .github/workflows/daily_run.yml:
Перейдіть на GitHub до вашого репозиторію

Знайдіть файл .github/workflows/daily_run.yml

Натисніть на нього та кнопку редагування (олівець)

Замініть весь вміст на:

name: Daily NBA Value Betting Report

on:
  schedule:
    - cron: '0 9 * * *'  # Run daily at 09:00 UTC
  workflow_dispatch:  # Allow manual trigger

jobs:
  run_bot:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Run NBA value betting bot
      run: python bot.py
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        NBA_API_KEY: ${{ secrets.NBA_API_KEY }}
